language: cpp
script:
 - mkdir -p build && cd build/ && cmake .. && cmake --build .
matrix:
    include:
        - os: linux
          dist: trusty
          sudo: false
          compiler: gcc
        - os: linux
          dist: trusty
          sudo: false
          compiler: clang
        - os: osx
          compiler: clang
